import VideoEditorBridge from '../../components/VideoEditorBridge';

export const metadata = {
  title: 'Agent OS — Video Editor',
  description: 'Embedded AI video editor bridge inside Agent OS.',
};

export default function VideoEditorPage() {
  return <VideoEditorBridge />;
}
